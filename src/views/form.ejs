<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Panel de ingreso</title>
    <style>
        *{
            margin:0; 
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }
        body{
            display: flex;
            background-color: #1d2b3a;
            min-height: 100vh;
            width: 100%;
            min-width: 330px;
            min-height: 645px;
            flex-direction: column;
            align-items: center;
        }
        header{
            display: flex;
            height: 60px;
            width: 100%;
            padding:10px; 
            align-items: center;
            justify-content: center;
            font-size: 22px;
            font-weight: 600;
            letter-spacing: 5px;
            color: #00dfc4; 
            border-bottom: 1px solid #00dfc4;
            background-color: #2e3c4b;
        }
        form{
            display: flex;
            justify-content: flex-start;
            align-items: center;
            min-height: 300px;
            padding-top: 120px;
            flex-direction: column;
            gap: 30px;
            background-color: #1d2b3a;
        }
        .inputBox{
            position: relative;
            width: 300px;
        }
        .inputBox input{
            width: 100%;
            padding: 14px;
            border: 1px solid rgba(255,255,255, 0.25);
            background: #1d2b3a;
            border-radius: 5px;
            outline: none;
            color: #eee;
            font-size: 1.8em;
            transition:0.65s;
        }
        
        .inputBox span{
            position:absolute;
            left: 0;
            padding: 14px ;
            pointer-events: none;
            font-size: 1.3em;
            color:rgba(255,255,255, 0.25); 
            text-transform: uppercase;
            transition: 0.65s;
        }
        .inputBox input:valid ~span,
        .inputBox input:focus ~span{
            color: #00dfc4; 
            transform: translateX(10px) translateY(-7px);
            font-size: 0.75em;
            padding:0 10px;
            background:#1d2b3a;
            border-left: 1px solid #00dfc4;
            border-right:1px solid #00dfc4; 
            letter-spacing: 0.2em;      
        } 
        .inputBox input:valid,
        .inputBox input:focus{
            border:1px solid #00dfc4; 
        }

        .btn{
            width: 300px;
            padding:12px ; 
            font-size: 1.7em;
            font-weight: 600;
            border-radius: 5px;
            text-transform: uppercase;
            background-color: #00dfc4;
            color: #1d2b3a;
            transition:0.5s;
            margin-top: 30px;
            margin-bottom: 50px;
            border:1px solid #2e3c4b;
        }
        .btn:hover{
            background-color: #1d2b3a;
            color:#00dfc4;
            cursor:pointer;
        }

        footer{
            color: #00dfc4;
            border:1px solid #fff; 
            border-radius: 50%;
            position: absolute;
            bottom: 30px;
            padding:50px 20px 10px 20px;
            margin:15px; 
            height: 210px;
            max-width: 210px;
            font-size: 14px;
            text-align: center;
            line-height: 25px;
        }
    </style>
    <link rel="icon" href="codingbrowser_102152.ico">
  </head>
  <body>
    <header>
        <p>UAGRM - INGRESO</p>
    </header>
    <form action="/notas" method="post">
      <div class="inputBox">
          <input class="reg" type="number" name="registro" required  min="121111111" max="299999999"/>
          <span>Nro registro</span>
      </div>
      <input class="btn" type="submit" value="Login" />
    </form>
    <footer>
        <p>
            Aqui podras ver tus datos personales y notas durante el transcurso del semestre.. :)
        </p>
    </footer>

  </body>
</html>
